name: webrtc_cli
description: WebRTC 桌面端 FFI 绑定库(仅 PC), API 命名与参数风格对齐 flutter_webrtc/lib/src。
version: 0.0.1

environment:
  sdk: ^3.13.0-167.1.beta

dependencies:
  ffi: ^2.1.0
  webrtc_interface: ^1.3.0

dev_dependencies:
  ffigen: ^21.0.0
  lints: ^6.0.0
  test: ^1.25.6
